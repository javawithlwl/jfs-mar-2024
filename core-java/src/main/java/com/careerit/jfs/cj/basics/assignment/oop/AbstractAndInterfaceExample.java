package com.careerit.jfs.cj.basics.assignment.oop;

interface Parser{

    public void init();
    void parse(String data);

}

// Create JPMCParser, CityBankParser, HDFCParser classes that implements Parser interface and init method should print the class name
// When parse method is called it should print the data.
public class AbstractAndInterfaceExample {
}
